import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
//to loads the js code to the 'root' div in index.html
ReactDOM.render(<React.StrictMode><App /></React.StrictMode>,document.getElementById('root'));

